/**
 * @module Dashboard/ducks
 */

import update from 'immutability-helper'

const initialState = {}

/**
 * Reducer
 * @param {state} [state=initialState] - Dashboard state or initial state
 * @param {object} action - the action type and payload
 */
export default function reducer (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
